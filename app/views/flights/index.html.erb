<h1>Flights</h1>

<% @flights.each do |f|%>
    <div class="flights-info">
        <h4>from: <%= link_to f.from_airport.code, airport_path(f.from_airport_id)%>, to: <%= link_to f.to_airport.code, airport_path(f.to_airport_id)%>, leave date: <%= f.start_date%></h4>
    </div>
<% end %>